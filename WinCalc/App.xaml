<Application x:Class="WinCalc.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WinCalc"
             xmlns:materialDesign="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             StartupUri="MainWindow.xaml">

    <Application.Resources>
        <ResourceDictionary>

            <!-- 🔹 Підключення тем і палітри -->
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Dark"
                                             PrimaryColor="Teal"
                                             SecondaryColor="Lime" />

                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- ✅ Глобальні кути -->
            <CornerRadius x:Key="MaterialDesignCornerRadius">20</CornerRadius>

            <!-- 🔹 Глобальний стиль для всіх вікон -->
            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            </Style>

            <!-- 🧱🔹 Контролли -->
          
            <!-- CheckBox -->
            <Style TargetType="CheckBox" BasedOn="{StaticResource MaterialDesignCheckBox}">  </Style>


            <!-- 🔹 Глобальний стиль для кнопок -->
            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>

              
                <Setter Property="FontSize" Value="14"/>
            </Style>

        

        </ResourceDictionary>
    </Application.Resources>
</Application>
